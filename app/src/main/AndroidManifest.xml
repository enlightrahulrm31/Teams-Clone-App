<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bcd">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.wifi" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bcd">
        <activity android:name=".WelcomeActivity"></activity>
        <activity android:name=".TeamRecyclerViewActivity"></activity>
        <activity android:name=".DestinationActivity" />
        <activity android:name=".ManageMeetActivity" />
        <activity android:name=".BoardingActivity">

            <!-- putting intents to check Boarding Activity -->
        </activity>
        <activity android:name=".ReclyclerViewActivity">

            <!-- you can keep intent here to run recylcer view activity -->
        </activity>
        <activity android:name=".DashboardActivity"></activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SpalshActivity"> <!-- THIS ACTIVITY IS  SPLASH ACTIVITY   here we will put intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <receiver android:name=".AlarmReciever" />
    </application>

</manifest>